# Сборка
/antibot
/dist/

# Состояние работающей ноды: не должно попадать в репозиторий никогда.
# Здесь события с чужими адресами, приватные ключи и токен подписки.
/var/
/events/
/facts/
/state/
*.ndjson
*.pem
*.key

# Локальная конфигурация
config.yaml
config.local.yaml
deploy/.env

.DS_Store
